FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir playwright requests beautifulsoup4
RUN playwright install --with-deps chromium

COPY facebook_scraper.py .

CMD ["python", "facebook_scraper.py", "watch", "60"]
